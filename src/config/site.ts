export type SiteConfig = {
    name: string,
    homeUrl: string,
    ownerEmail: string
}

export const site = {
    name: "Portfolio de Kristofer Gehringer",
    homeUrl: "/portfolio",
    ownerEmail: "kristofer.gehringer@gmail.com"
} as SiteConfig
