export type SiteConfig = {
    base: string,
    name: string,
    ownerName: string,
    ownerEmail: string,
    githubUrl: string,
    githubHandle: string,
    linkedinUrl: string,
    linkedinHandle: string,
    repositoryUrl: string,
}
